# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.iml

# Kiro（steering/ と skills/ はチーム共有するため Git 管理する）
.kiro/settings/

# Skills CLI (他エディタ向けのユニバーサル出力。Kiro は .kiro/skills/ を使用)
.agents/

# clasp (contains script ID)
.clasp.json

# ---- Refactoring (new stack) ----

# Backend (Kotlin + Gradle)
backend/.gradle/
backend/build/
backend/.kotlin/
backend/out/

# Frontend (Next.js + pnpm)
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.turbo/
# 万一コンテナ外で pnpm を叩いた場合にストアがプロジェクト配下に作られても Git に入れない
frontend/.pnpm-store/

# Monorepo
node_modules/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Docker
docker-compose.override.yml
